# Golden files for TUI behavioral tests
# Preserve line endings exactly as stored (no CRLF conversion)
*.golden -text
# Hide golden file diffs in GitHub PRs by default (click to expand)
*.golden linguist-generated=true
